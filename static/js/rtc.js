/****
*
* Handled by the client
*
*****/

exports.handleClientMessage_RTC = function(hook, context){
  var action = cntext.message.data.action;
alert("pow");
  if(action === "request"){
alert("request from someone");
  }
  if(action === "approve"){

  } 
  if(action === "deny"){
    
  }
}

